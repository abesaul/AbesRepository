name: One Piece Monitor

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: pip install -r requirements.txt
    
    - name: Load previous data
      uses: actions/cache@v3
      with:
        path: known_products.json
        key: products-${{ github.run_id }}
        restore-keys: products-
    
    - name: Run monitor
      env:
        DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
      run: python monitor_once.py
    
    - name: Save data
      uses: actions/cache@v3
      with:
        path: known_products.json
        key: products-${{ github.run_id }}
